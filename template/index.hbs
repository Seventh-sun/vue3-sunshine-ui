import Sun{{properCase name}} from './src/{{name}}.vue'

Sun{{properCase name}}.install = Vue => {
    Vue.component(Sun{{properCase name}}.name, Sun{{properCase name}})
}

export default Sun{{properCase name}}